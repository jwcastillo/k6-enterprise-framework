{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "k6 Enterprise Framework — Client Configuration",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "client"
  ],
  "properties": {
    "client": {
      "type": "string",
      "pattern": "^[a-zA-Z0-9_-]+$"
    },
    "version": {
      "type": "string",
      "pattern": "^\\d+\\.\\d+\\.\\d+$"
    },
    "description": {
      "type": "string"
    },
    "environment": {
      "type": "string",
      "enum": [
        "default",
        "staging",
        "production",
        "rr",
        "st",
        "prod"
      ]
    },
    "baseUrl": {
      "type": "string",
      "format": "uri"
    },
    "allowedHosts": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "description": "Hostnames this client may fire load at. When set, bin/target-guard.js refuses any baseUrl whose host is not listed."
    },
    "maxVUs": {
      "type": "integer",
      "minimum": 1,
      "description": "Load ceiling (VUs) for AI-generated scenarios. bin/validate-generated.js warns above it, fails with --strict."
    },
    "maxRate": {
      "type": "integer",
      "minimum": 1,
      "description": "Load ceiling (iterations/s) for AI-generated scenarios. bin/validate-generated.js warns above it, fails with --strict."
    },
    "endpoints": {
      "type": "object",
      "additionalProperties": {
        "type": "object",
        "required": [
          "baseUrl"
        ],
        "properties": {
          "baseUrl": {
            "type": "string",
            "format": "uri"
          },
          "timeout": {
            "type": "string",
            "pattern": "^[0-9]+(ms|s|m|h)$"
          }
        },
        "additionalProperties": true
      }
    },
    "thresholds": {
      "type": "object",
      "additionalProperties": {
        "type": "array",
        "items": {
          "type": "string"
        },
        "minItems": 1
      }
    },
    "scenarios": {
      "type": "object",
      "additionalProperties": {
        "type": "object",
        "required": [
          "executor"
        ],
        "properties": {
          "executor": {
            "type": "string",
            "enum": [
              "constant-vus",
              "ramping-vus",
              "constant-arrival-rate",
              "ramping-arrival-rate",
              "per-vu-iterations",
              "shared-iterations",
              "externally-controlled"
            ]
          },
          "vus": {
            "type": "integer",
            "minimum": 1
          },
          "duration": {
            "type": "string",
            "pattern": "^[0-9]+(ms|s|m|h)$"
          }
        },
        "additionalProperties": true
      }
    },
    "auth": {
      "type": "object",
      "required": [
        "type"
      ],
      "properties": {
        "type": {
          "type": "string",
          "enum": [
            "none",
            "bearer",
            "basic",
            "api-key"
          ]
        }
      },
      "additionalProperties": true
    },
    "retries": {
      "type": "object",
      "properties": {
        "enabled": {
          "type": "boolean"
        },
        "maxRetries": {
          "type": "integer",
          "minimum": 0,
          "maximum": 10
        },
        "retryOn": {
          "type": "array",
          "items": {
            "type": "integer"
          }
        },
        "backoffMs": {
          "type": "integer",
          "minimum": 0
        }
      },
      "additionalProperties": false
    },
    "browser": {
      "type": "object",
      "additionalProperties": true
    },
    "tags": {
      "type": "object",
      "additionalProperties": {
        "type": "string"
      }
    }
  }
}
