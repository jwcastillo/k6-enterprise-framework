#!/usr/bin/env sh
# T-069: Commitlint hook — validates commit messages follow Conventional Commits
# Bypass: git commit --no-verify (emergency only, document in team channel)
. "$(dirname "$0")/_/husky.sh"

npx --no -- commitlint --edit "$1"
